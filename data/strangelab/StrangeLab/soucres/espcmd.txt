
  Commands you can run

  export NODE_ID=lab-esp32-01
  export WIFI_SSID=YourSSID
  export WIFI_PASS=YourPass
  export INGEST_URL=http://pi-logger.local:8088/v1/ingest

  pio run -e esp32dev
  pio run -e esp32dev -t upload --upload-port /dev/cu.usbserial-0001

  export NODE_ID=lab-esp32-c3-01
  export WIFI_SSID=YourSSID
  export WIFI_PASS=YourPass
  export INGEST_URL=http://pi-logger.local:8088/v1/ingest

  pio run -e esp32c3
  pio run -e esp32c3 -t upload --upload-port /dev/cu.usbserial-0002
